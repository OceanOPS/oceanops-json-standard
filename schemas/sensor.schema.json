{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"type": "object",
	"properties": {
	  "id": {
		"type": ["integer", "null"],
		"description": "Unique identifier for the sensor"
	  },
	  "name": {
		"type": "string",
		"description": "Full name of the sensor"
	  },
	  "name_short": {
		"type": "string",
		"description": "Short name or abbreviation of the sensor"
	  },
	  "link_url": {
		"type": "string",
		"format": "uri",
		"description": "URL link related to the sensor"
	  },
	  "image_url": {
		"type": "string",
		"format": "uri",
		"description": "URL of an image of the sensor"
	  },
	  "bodc_id": {
		"type": "string",
		"description": "British Oceanographic Data Centre (BODC) identifier for the sensor"
	  },
	  "description": {
		"type": "string",
		"description": "Detailed description of the sensor"
	  },
	  "yearly_drift": {
		"type": ["number", "null"],
		"description": "Annual drift of the sensor measurements"
	  },
	  "axes_amount": {
		"type": ["integer", "null"],
		"description": "Number of axes the sensor measures"
	  },
	  "agency": {
		"type": "object",
		"description": "Agency associated with the sensor"
	  },
	  "networks": {
		"type": "array",
		"description": "Networks the sensor is part of"
	  },
	  "types": {
		"type": "array",
		"items": {
		  "type": "object",
		  "properties": {
			"id": {
			  "type": "integer",
			  "description": "Unique identifier for the sensor type"
			},
			"name": {
			  "type": "string",
			  "description": "Full name of the sensor type"
			},
			"name_short": {
			  "type": "string",
			  "description": "Short name or abbreviation of the sensor type"
			},
			"description": {
			  "type": "string",
			  "description": "Detailed description of the sensor type"
			},
			"variable": {
			  "type": "object",
			  "properties": {
				"id": {
				  "type": "integer",
				  "description": "Unique identifier for the variable measured"
				},
				"name": {
				  "type": "string",
				  "description": "Name of the variable measured"
				},
				"description": {
				  "type": "string",
				  "description": "Detailed description of the variable measured"
				}
			  },
			  "required": ["id", "name"],
			  "additionalProperties": false
			}
		  },
		  "required": ["id", "name", "variable"],
		  "additionalProperties": false
		}
	  }
	},
	"required": ["id", "name", "types"],
	"additionalProperties": false
  }